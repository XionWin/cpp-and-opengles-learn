{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: gcc build active file",
            "command": "make",
            "args": [
                // "-g",
                // // "${file}",
                // "main.c",
                // "-o",
                // // "${fileDirname}/${fileBasenameNoExtension}"
                // "main",
                // "-g",
                // // "${file}",
                // "framebuffer.c",
                // "-o",
                // // "${fileDirname}/${fileBasenameNoExtension}"
                // "framebuffer.o"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}